<controls:MetroWindow x:Class="Spawn.HDT.DustUtility.UI.Dialogs.HistoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:vm="clr-namespace:Spawn.HDT.DustUtility.ViewModel"
        xmlns:uicontrols="clr-namespace:Spawn.HDT.DustUtility.UI.Controls"
        mc:Ignorable="d" WindowStartupLocation="CenterOwner" BorderBrush="{DynamicResource AccentColorBrush}"
        Title="{Binding WindowTitle}" Height="450" Width="700">
    <Window.DataContext>
        <vm:HistoryDialogViewModel/>
    </Window.DataContext>
    <Grid>
        <uicontrols:CardsDataGrid ItemsSource="{Binding CardItems}" ColoredCountColumn="True" DateColumnVisible="True" RowDeleted="OnRowDeleted" Margin="0,0,0,30"/>
        <Button Content="Clear" Command="{Binding ClearHistoryCommand}" VerticalAlignment="Bottom" Height="30"/>
    </Grid>
</controls:MetroWindow>
